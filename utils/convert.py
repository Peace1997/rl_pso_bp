# 归一化的预测值
b = [0.1013, 0.1477, 0.5181, 0.7473, 0.8848]

# 最后五个真实输出值
c = [
    2,
    10.23,
    30.81,
    49.8,
    60.88,
]

# 最后一列的原最大最小值
e = [1.9, 61.91]

for i in range(len(b)):
    d = ((b[i] - 0.1) * 10 / 8) * (max(e) - min(e)) + min(e)
    print(d)
